import re

if __name__ == "__main__":
    num = int(input())
    for _ in range(0, num):
        pattern = input()
        try:
          res = re.compile(pattern)
          print(True)
        except re.error:
          print(False)

